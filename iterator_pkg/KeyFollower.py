import numpy as npimport h5pyimport osclass Follower():    def __init__(self,                 filepath = {},                 keypath = {}):        self.filepath = filepath        self.keypath = keypath        self._keyindex = 0                                    #Private method to load files into a dictionary need to safely close    def _load_files(self):                        self.files = {}        for dict_key in self.filepath.keys():            self.files[dict_key] = h5py.File(self.filepath, "r")            self.files[dict_key][self.keypath[dict_key]]                            def _close_files(self):        for file in self.files:            file.close()                    def _refresh_files(self):        for file in self.files:            file.refresh()    def _load_key_datasets(self):                #return a dictionary of key datasets        #self.key_datasets                pass    def current_max(self):        max_array = np.array([])        self._refresh_files()        for dataset in self.key_datasets:            temp = np.asarray(dataset).flatten()            temp_max = (np.nonzero(temp)[0]).max()            max_array += temp_max        return max_array.min()                            def plot_keys(self):        pass                def current_key(self):        return(self._keyindex)            def is_next(self):            pass                