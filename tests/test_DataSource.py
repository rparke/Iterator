import numpy as npimport h5pyimport pytestimport osimport iterator_pkgfrom iterator_pkg import DataSourcefrom iterator_pkg import KeyFollower#Check KeyFollower can translate a dictionary keypath and filepath pairs into a dictionary of key arraysdef test_iterates_complete_dataset():                filepath = "/Users/richardparke/Documents/Diamond/Iterator/tests/hdf5_tests/i18-81742.nxs"      key_paths = ["entry/solstice_scan/keys/PANDABOX.h5"]      data_paths = ['entry/Xspress3A/data']      with h5py.File(filepath, "r") as f:          #data = f[key_paths[0]][...]           kf = DataSource.DataFollower(f, key_paths, data_paths, timeout = 1)           kf._get_frame_shape()           current_key = 0           for key in kf:               print(key)               print(len(key[0]))               current_key+= 1                assert current_key == 2814      print(current_key == 2814)            